# 组件目录的CMakeLists.txt

# 这里可以添加项目中的C++组件
# 例如：点云处理组件、SAR数据处理组件、形变分析组件等

# 未来组件示例（目前为空）：
# add_subdirectory(point_cloud_processing)
# add_subdirectory(insar_processing)
# add_subdirectory(deformation_analysis)

message(STATUS "组件目录初始化完成，等待添加具体组件") 